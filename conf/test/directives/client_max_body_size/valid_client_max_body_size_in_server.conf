http {
    server {
        listen 8080;
        server_name localhost;
        client_max_body_size 1000;

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
