http {

    server {
        listen 8080;
        server_name localhost;

        location / {
            client_max_body_size 10;
            root /var/www/html;
            index index.html;
            client_max_body_size 30;
        }
    }

}
