http {    
    # これはサーバーブロックの前のコメントです
    # 複数行に渡っています
    server {
        listen 80;
        # これはlocationブロックの前のコメントです
        # これも複数行です
        location / {
            # ルートディレクティブの前のコメント
            root /var/www/html;
        }
    }
}