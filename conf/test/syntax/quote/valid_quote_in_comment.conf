http {
    server {
        server_name example.com;  # 'single quotes' と "double quotes" がコメント内にあっても問題なし
        root /var/www/html;       # コメント内のクォート "quotes" は構文解析に影響を与えない
        listen 80;
    }
}